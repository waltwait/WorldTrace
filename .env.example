# Copy to .env.local and fill in your own Web OAuth client ID.
# Without it the Backup screen's cloud section reads 「尚未設定」 and local
# export still works. See the README for how to create one.
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
